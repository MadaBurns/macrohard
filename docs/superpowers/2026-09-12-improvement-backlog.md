# Improvement backlog — 2026-09-12

**Scope:** read-only audit of this repo at `fd5c138` plus read-only probes of the live site and the public GitHub API. No source, test, or config file was changed. **Doctrine:** bv-cc @ f9535a8.

**Redaction.** The second collaborator in T2 is referred to as `<login>`; the operator can reproduce the check from the collaborators endpoint. Their login does not belong in a public document.

**Method.** Read README.md, both files under `docs/superpowers/`, the full git log (12 commits, all 2026-09-12), `src/*.ts`, `public/index.html`, `public/method.html`, `public/app.js`, `scripts/*.sh`. Ran the command chain below. Probed https://macrohard.nz, https://www.macrohard.nz and `api.github.com` with `curl` and `gh` (GET only). One number was not measured and is marked as such.

Legend: **Impact** is against the site's premise (every published sentence is true and every figure is reproducible by a stranger). **Effort** S = under an hour, one PR; M = half a day; L = more. **OPERATOR** = needs secrets, the Cloudflare account, a deploy, or GitHub admin — an agent must not attempt it.

## 1. Command chain — every non-green result, verbatim

The chain as written halts at `format:check`, so `npm outdated` and `npm audit` were run separately afterwards.

```
npm ci && npm run types && npm run typecheck && npm test && npm run format:check
→ exit 1 (halted at format:check)
```

**`npm run typecheck` on a clean clone** (observed earlier this session, before `wrangler types` had ever run in this checkout; passes once `npm run types` has run):

```
error TS2688: Cannot find type definition file for './worker-configuration.d.ts'.
  The file is in the program because:
    Entry point of type library './worker-configuration.d.ts' specified in compilerOptions
```

**`npm test`** — green (`Test Files 12 passed (12)`, `Tests 84 passed (84)`) but with three warnings worth recording:

```
[vpw:warn] Workers AI and Vectorize bindings will access your Cloudflare account and incur usage charges even in testing. We recommend mocking any usage of these bindings in your tests.
[mf:warn] The latest compatibility date supported by the installed Cloudflare Workers Runtime is "2025-09-06",
receipts refresh failed MockNotMatchedError: Mock dispatch not matched for path '/repos/MadaBurns/bv-mcp/commits?per_page=100&since=…': subsequent request to origin https://api.github.com was not allowed (net.connect disabled)
```

(The third is the hermetic guard working as intended; the first two are findings E7 and E8.)

**`npm run format:check`:**

```
[warn] docs/superpowers/2026-09-12-e2e-results.md
[warn] Code style issues found in the above file. Run Prettier with --write to fix.
```

**`npm outdated`** (exit 1):

```
Package                          Current   Wanted  Latest
@cloudflare/vitest-pool-workers   0.8.71   0.8.71  0.22.0
typescript                         5.9.3    5.9.3   7.0.2
vitest                             3.2.7    3.2.7   5.0.0
zod                              3.25.76  3.25.76   4.6.2
```

**`npm audit`** (exit 1): `10 vulnerabilities (2 moderate, 8 high)`.

```
@vitest/mocker  2.1.0 - 4.1.10   moderate  GHSA-82fw-gwwq-j7x9  fix: vitest@5.0.0 (breaking)
extract-zip     *                high      GHSA-jmr9-qjv8-65gv, GHSA-7pqw-9j4j-h8q3  via @puppeteer/browsers ← @cloudflare/puppeteer  "fix": @cloudflare/puppeteer@0.0.11 (a downgrade)
sharp           <=0.35.4-rc.0    high      GHSA-f88m-g3jw-g9cj, GHSA-rgj7-g3m4-5g8c  via miniflare ← @cloudflare/vitest-pool-workers  fix: @cloudflare/vitest-pool-workers@0.22.0 (breaking)
ws              8.0.0 - 8.20.1   high      GHSA-58qx-3vcg-4xpx, GHSA-96hv-2xvq-fx4p  via miniflare  fix: same
```

Every advisory is in a devDependency or in puppeteer's local-browser download path. None is in code that runs inside the deployed Worker. See §6.

## 2. Findings

### Dimension 1 — Truthfulness

| ID  | Finding                                                                                                                                                                                                                                                                                                                                                                                                                         | Evidence                                                                                                                                                        | Impact | Effort               | Proposed change                                                                                                                                                                                                                           |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1  | The Method page's Scope says **"Three public repositories"** and then lists four; the config has four; the live snapshot reports four.                                                                                                                                                                                                                                                                                          | `public/method.html:40` vs `wrangler.jsonc` `GITHUB_REPOS` (4 names); live `/api/receipts` `repos` = `[bv-mcp, blackveil-dns-action, bv-claude-dns, macrohard]` | High   | S                    | Fix the word, and add a test in `test/assets.spec.ts` that the number of `github.com/MadaBurns/` links under `#scope` equals the configured repo count so it cannot drift again.                                                          |
| T2  | The Method page says **"The repositories have one human maintainer with commit access."** One of the four repos has a second collaborator with `write` permission. They made no commits in the window, so the _figures_ are unaffected, but the sentence is false as written and the page invites exactly this check.                                                                                                           | `public/method.html:76`; `gh api repos/MadaBurns/bv-mcp/collaborators/<login>/permission` → `write`; `…/commits?author=<login>&since=-90d` → 0                  | High   | S (copy) / OPERATOR  | Reword to what is provable: "one human has committed in the window and holds the deploy keys". Removing the second collaborator's write access is the operator's call, not the agent's.                                                   |
| T3  | README says **"Nothing reaches a public permalink unmoderated."** When Llama Guard errors, the verdict is `unknown`, treated as pass, and the generation is stored at a permalink and cached. The spec discloses the degrade; the README sentence is absolute.                                                                                                                                                                  | `src/staff.ts` `moderateQuery`/`moderateOrg` catch → `'unknown'`; `src/index.ts` tests only `=== 'unsafe'`; README "Spend controls" item 5                      | High   | M                    | On `unknown` from either guard: serve the org but do not write `s:<id>` or the query cache and return `id: null` (the client already hides Post/Copy when `id` is absent, `public/app.js:239`). Then the README sentence is true. See L4. |
| T4  | Hero: agents **"review each other's pull requests"**. Nothing on the page or in `src/receipts.ts` measures reviews; the spec's own rule is that the hero claims only what `git log` supports, and reviews are not in `git log`.                                                                                                                                                                                                 | `public/index.html:49`; no `/reviews` call anywhere in `src/`                                                                                                   | Medium | S (copy) / M (prove) | Either drop "review each other's pull requests" from the hero, or add a receipted figure from `GET /repos/{o}/{r}/pulls/{n}/reviews` with its rule on the Method page. Do not keep an unmeasured claim next to measured ones.             |
| T5  | Method: **"refreshed every 30 minutes"**. True today (cron `*/30`; live snapshot `08:30:03Z` observed at `08:48Z`), but only while `GITHUB_TOKEN` is valid. On expiry the cron degrades to anonymous and loses to shared egress IPs (ledger F4) and the sentence reverts to intent. The code's actual guarantee — refresh by cron, and on any visit once the snapshot is over 45 minutes old — is true regardless of the token. | `wrangler.jsonc` `triggers.crons`; `src/index.ts` `RECEIPTS_MAX_AGE_MS`; `src/receipts.ts` `ghFetch` fallback; ledger F4/F6                                     | Medium | S                    | Change the copy to the guarantee the code makes: "refreshed every 30 minutes, and on the first visit after 45". Pair with B1 so the front page shows when the token has lapsed.                                                           |
| T6  | Hero: **"Every figure on this page links to the public commit that proves it."** Three figures do not: Headcount human (hard-coded `1`), Meetings held (hard-coded `0`), and the stats link to the Method page rather than a commit. The Method page discloses the first two.                                                                                                                                                   | `public/index.html:50-51,64,96`                                                                                                                                 | Low    | S                    | "Every figure on this page is computed from the public GitHub API, or the Method page says why not."                                                                                                                                      |
| T7  | Docs drift: README says `STAFF_DAILY_CAP` default 400 (config: 1500); the design spec says "41 tests" (84 now) and "Three public repositories"; README's tree omits `src/og.ts`.                                                                                                                                                                                                                                                | `README.md` "Spend controls" item 3; `docs/superpowers/specs/…design.md` "Tests", "Constraints"; `wrangler.jsonc` `STAFF_DAILY_CAP`                             | Low    | S                    | Replace numbers that rot with pointers (`npm test`, `wrangler.jsonc`), and list `og.ts`.                                                                                                                                                  |
| T8  | A test named **"redirects www even for asset paths"** requests `/method`, which is not an asset. Since F7 scoped `run_worker_first`, real asset paths on `www.` do not redirect: live `www.macrohard.nz/styles.css` → `200`, no `Location`. The catch-all's comment still says "run_worker_first is on so the www redirect above sees every request".                                                                           | `test/assets.spec.ts:32-36`; `src/index.ts` comment above `app.get('*')`; `curl -sI https://www.macrohard.nz/styles.css` → `HTTP/2 200`                         | Low    | S                    | Rename the test to what it checks, fix the comment, and add one line to the spec: www→apex applies to Worker-handled paths only. No published URL uses `www.` for an asset, so no behaviour change is needed.                             |
| T9  | 70 `dependabot[bot]` and 2 `Copilot` commits in bv-mcp's 90-day window sit in the "human" denominator. Rule-consistent and disclosed ("counted as human even if an agent helped"), but "human" is the wrong word for a bot.                                                                                                                                                                                                     | `gh api --paginate repos/MadaBurns/bv-mcp/commits?since=-90d` author counts: MadaBurns 360, Blackveil 123, dependabot[bot] 70, Copilot 2                        | Low    | S                    | Method page: "counted as _not agent-authored_" instead of "counted as human". Do not change the rule.                                                                                                                                     |

### Dimension 2 — Enforcement gaps

| ID  | Finding                                                                                                                                                                                                                                                 | Evidence                                                                                                      | Impact | Effort   | Proposed change                                                                                                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | No CI at all. The baseline chain is red today (§1) and nothing would tell anyone.                                                                                                                                                                       | No `.github/` in the tree; `gh workflow list` → empty                                                         | High   | S        | `.github/workflows/ci.yml`: Node 22, `npm ci && npm run types && npm run typecheck && npm test && npm run format:check` on `pull_request` and `push` to `main`. No deploy step, no secrets. Deploy stays operator-run. |
| E2  | `main` of a public repo has no branch protection and no rulesets.                                                                                                                                                                                       | `gh api repos/MadaBurns/macrohard/branches/main/protection` → 404 "Branch not protected"; `…/rulesets` → `[]` | High   | OPERATOR | Require the CI check from E1 and a PR before merge. GitHub admin; not for an agent.                                                                                                                                    |
| E3  | `npm run deploy` is a bare `wrangler deploy` with no pre-deploy gate; the only thing between a red test suite and production is the operator remembering.                                                                                               | `package.json` `scripts.deploy`                                                                               | Medium | S        | Add `"predeploy": "npm run typecheck && npm test"`. The operator still presses the button; the button just refuses when red.                                                                                           |
| E4  | `npm run typecheck` fails on a clean clone (§1) because `worker-configuration.d.ts` is generated and gitignored, and README tells a newcomer to run it.                                                                                                 | `tsconfig.json` `types`; `.gitignore`; README "Running it"                                                    | Medium | S        | `"pretypecheck": "wrangler types"` and one README sentence.                                                                                                                                                            |
| E5  | `format:check` is red on one ledger file.                                                                                                                                                                                                               | §1                                                                                                            | Low    | S        | Run `prettier --write` on it, or add `docs/superpowers/*.md` to `.prettierignore` with a one-line reason if the tables come out worse.                                                                                 |
| E6  | No Dependabot/Renovate config; 10 advisories have accumulated with no signal.                                                                                                                                                                           | No `.github/dependabot.yml`; §1 audit                                                                         | Medium | S        | `.github/dependabot.yml`, npm, weekly, grouped minor/patch. Majors still go through §6 one at a time.                                                                                                                  |
| E7  | The hermetic suite runs on a Workers runtime whose newest supported compatibility date is **2025-09-06**, while production is pinned to **2026-09-01**. Tests exercise a runtime a year behind the one that serves the site.                            | `[mf:warn]` in §1; `wrangler.jsonc` `compatibility_date`                                                      | Medium | M        | Bump `@cloudflare/vitest-pool-workers` (0.8 → 0.22) together with `vitest` (3 → 5). Same PR as P1/P2 in §6.                                                                                                            |
| E8  | The real `AI` binding is live inside the test pool. Today no test reaches `ai.run` through `SELF.fetch` (the `/api/staff` tests only hit the rejected and cached paths), but nothing enforces that, and the pool itself warns it will bill.             | `[vpw:warn]` in §1; `test/routes.spec.ts` `/api/staff` cases                                                  | Medium | S        | Override `AI` in `vitest.config.ts` (`miniflare.bindings` or a `wrappedBindings` stub that throws), so a future test that reaches the model fails loudly instead of billing quietly.                                   |
| E9  | The repo that presents itself as agent-operated has no `CLAUDE.md`/`AGENTS.md`; the rules that matter (never deploy, hermetic tests, trailer on every commit because the repo counts itself, no real people or companies in copy) live only in ledgers. | Tree listing                                                                                                  | Low    | S        | A ten-line `CLAUDE.md` at the root with those rules and the baseline command. Pointers, not narrative.                                                                                                                 |

### Dimension 3 — Dead or untested surface

| ID  | Finding                                                                                                                                                                                                                                                                                                                        | Evidence                                                                                                       | Impact | Effort | Proposed change                                                                                                                                                                                                                    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- | ------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | `GET /api/presets` is served, has no caller (the preset chips are hard-coded `data-q` attributes) and no test.                                                                                                                                                                                                                 | `src/index.ts:224`; `public/index.html:143-146`; `public/app.js:253-258`; `grep -rn presets test/` → nothing   | Low    | S      | Remove the route. If the chips should ever be data-driven, that is a new feature with its own test.                                                                                                                                |
| D2  | The `scheduled` handler — the thing F4 spent a day on — has no test.                                                                                                                                                                                                                                                           | `grep -rln scheduled test/` → nothing                                                                          | Medium | S      | One test: `scheduled()` calls `refreshReceipts`, and a thrown refresh is logged and swallowed (previous snapshot intact).                                                                                                          |
| D3  | `public/app.js` (receipts render, permalink hydration, counter, fallback note, share links) has no automated test; it was verified once by hand with Playwright (ledger layer 2).                                                                                                                                              | `test/` contains no DOM test                                                                                   | Low    | M      | A small jsdom or Playwright smoke in CI against `wrangler dev` with the AI binding stubbed. Only worth it once E1 exists.                                                                                                          |
| D4  | Concurrent stale refreshes: every `/api/receipts` request during the stale window schedules its own `refreshReceipts` (~20 GitHub calls each). Edge `max-age=60` helps per-POP only. Harmless with a valid token (5,000/hr); once the token lapses, a burst of visitors at a stale moment burns the anonymous 60/hr in one go. | `src/index.ts` `/api/receipts` `waitUntil(refreshReceipts…)` with no lock; contrast `oglock:` in `prerenderOg` | Medium | S      | A `refreshlock` KV key (120 s, same pattern as `oglock:`) so at most one refresh is in flight per window. Not a strict guarantee (KV), but collapses the dominant case exactly as the OG lock does.                                |
| D5  | `MAX_PAGES = 10` silently truncates any repo above 1,000 commits in the window. bv-mcp is at 555 today. The Method page says "following `Link: rel="next"`" with no cap, so a truncated number would be presented as complete.                                                                                                 | `src/receipts.ts` `MAX_PAGES`; `gh api …/commits?since=-90d&per_page=1` → `rel="last"` page 555                | Medium | S      | Carry a `truncated: string[]` in the snapshot when the page cap is hit, show it on the front page ("bv-mcp: first 1,000 of the window"), and log it. Or raise, so the previous snapshot is kept. Never present a floor as a total. |

### Dimension 4 — Documented-not-fixed items from the e2e ledger

| ID  | Item                                   | Cheap strict improvement?                                                                                                                                                                                                                                                                | Verdict                 |
| --- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| L1  | F3 per-IP rate limit is approximate    | No. The binding counts per location by design; the daily cap is the hard bound. A Durable Object would be exact but is a new binding.                                                                                                                                                    | Leave; keep documented. |
| L2  | KV daily-cap counter can overshoot     | No. KV has no compare-and-swap; the overshoot is bounded by concurrency and the cap's purpose is "not unbounded".                                                                                                                                                                        | Leave; keep documented. |
| L3  | `oglock:` check-then-set window        | No cheap strict fix; reordering the lock and the budget consume does not close the window. The strict fix is a DO. The daily OG budget bounds the damage.                                                                                                                                | Leave; keep documented. |
| L4  | Guard outage (`unknown`) reads as pass | **Yes.** Keep serving (an outage must not take the toy down) but stop _persisting_: no `s:<id>`, no cache, `id: null`. The client already handles a missing id. That makes README's sentence true (T3) at the cost of "no permalink while the guard is down", which is the honest trade. | Fix. Ranked P5 below.   |
| L5  | `count:total` bump is non-atomic       | Same as L2; it is labelled "a counter, not a ledger" in code.                                                                                                                                                                                                                            | Leave.                  |

### Dimension 5 — Dependency currency and advisories

| Package                           | Current | Latest | Runtime? | Advisory                                                        | Migration risk                                                                                                                                                                                     |
| --------------------------------- | ------- | ------ | -------- | --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `vitest`                          | 3.2.7   | 5.0.0  | dev      | moderate (GHSA-82fw-gwwq-j7x9, `@vitest/mocker` path traversal) | Two majors. Must move in lockstep with the pool package; read both changelogs first. Config surface here is tiny (`vitest.config.ts` is 14 lines).                                                 |
| `@cloudflare/vitest-pool-workers` | 0.8.71  | 0.22.0 | dev      | high ×2 via nested `miniflare` (`sharp`, `ws`)                  | Clears 6 of the 8 highs and closes E7 (runtime currency). Same PR as vitest. Watch for `wrangler` peer version.                                                                                    |
| `zod`                             | 3.25.76 | 4.6.2  | **yes**  | none                                                            | Major. `.trim().min().max()` chains survive; error object shape and `z.infer` edge cases changed. Low urgency; one PR, run `test/staff.spec.ts` hard.                                              |
| `typescript`                      | 5.9.3   | 7.0.2  | dev      | none                                                            | Major (new compiler). `strict` code should pass; low urgency; last.                                                                                                                                |
| `@cloudflare/puppeteer`           | current | —      | **yes**  | high ×2 via `@puppeteer/browsers` → `extract-zip`               | The "fix" `npm audit` offers is a _downgrade_ to 0.0.11. Do not take it. The vulnerable path is local browser download, never executed in the Worker. Track upstream; suppress with an audit note. |
| `hono`, `wrangler`, `prettier`    | current | —      | —        | none                                                            | Nothing to do.                                                                                                                                                                                     |

### Dimension 6 — Operational time bombs

| ID  | Finding                                                                                                                                                                                                                                                                                                                     | Evidence                                                                  | Impact | Effort       | Proposed change                                                                                                                                                                                                                                                                                                                                          |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------ | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | `GITHUB_TOKEN` was minted **2026-09-12** (ledger F4 closure); fine-grained PATs cap at 366 days, so it lapses by **2027-09-13** at the latest. Nothing in the repo, the Worker, or `watch.sh` will notice. The site degrades to F4 behaviour (stale-but-serving), not an outage — which is exactly why nobody would notice. | ledger F4; `src/receipts.ts` `ghFetch`                                    | Medium | S / OPERATOR | In `ghFetch`, read `x-ratelimit-limit` from the first response (5000 = authenticated, 60 = anonymous) and store `authenticated: boolean` in the snapshot; `watch.sh` and `/api/receipts` surface it, and the front page's "As of" line can say "unauthenticated" when false. Rotation itself is OPERATOR; a calendar entry for 2027-08 is theirs to set. |
| B2  | Permalinks and their share images expire after **180 days** (`PERMALINK_TTL`). The three Day-0 permalinks in the ledger, and every link shared at launch, 404 from about **2027-03-11**. A shared link that dies is worse than one that was never shared.                                                                   | `src/index.ts` `PERMALINK_TTL = 180 * 86_400` on both `s:` and `og:` puts | Medium | S            | Drop the TTL on `s:` (an org is about 1 KB; KV storage is not the constraint) or raise it to years. Keep the TTL on `og:` (re-renderable) if minutes matter. Update the spec's "for the permalink's lifetime".                                                                                                                                           |
| B3  | The per-query cache TTL is 30 days, so a repeat prompt after a month costs a generation again while its old permalink still exists. Not a bug; a mild spend inefficiency.                                                                                                                                                   | `src/index.ts` cache put `expirationTtl: 30 * 86_400`                     | Low    | S            | Align with B2 or leave.                                                                                                                                                                                                                                                                                                                                  |
| B4  | The `macrohard.nz` registration renewal date is not recorded anywhere an agent can read. **Not measured** this session.                                                                                                                                                                                                     | —                                                                         | High   | OPERATOR     | Record the renewal date in the ledger and set auto-renew. The offer on the front page is only credible while the domain is held.                                                                                                                                                                                                                         |
| B5  | Share-card rendering waits on Google Fonts (`networkidle0` + `document.fonts.ready`); a slow font CDN stretches each render toward the Browser Rendering minutes budget rather than failing fast.                                                                                                                           | `src/og.ts` `FONTS`, `renderOgPng`                                        | Low    | S            | Set an explicit `timeout` on `setContent` (for example 8 s) so a slow CDN falls back to the generic card instead of consuming budget.                                                                                                                                                                                                                    |

## 3. Ranked backlog

Ranked by impact, then effort. OPERATOR items are listed for completeness and must not be attempted by an agent.

| #   | ID         | One line                                                                                                     | Impact | Effort   |
| --- | ---------- | ------------------------------------------------------------------------------------------------------------ | ------ | -------- |
| 1   | T1         | Method page says three repos; there are four. Fix and pin with a test.                                       | High   | S        |
| 2   | T2         | "One human maintainer with commit access" is false for one repo. Reword.                                     | High   | S        |
| 3   | E1         | Add CI running the baseline chain.                                                                           | High   | S        |
| 4   | E4+E5+E3   | Make the baseline green on a clean clone: `pretypecheck`, format, `predeploy`.                               | High   | S        |
| 5   | T3/L4      | Guard `unknown` must not persist a permalink; then README's sentence is true.                                | High   | M        |
| 6   | B2         | Permalinks expire in 180 days; launch links will die. Remove the TTL.                                        | Medium | S        |
| 7   | T5         | "Refreshed every 30 minutes" → state the guarantee the code makes.                                           | Medium | S        |
| 8   | D4         | Lock concurrent stale refreshes (`refreshlock`, same pattern as `oglock`).                                   | Medium | S        |
| 9   | B1         | Surface whether the GitHub token is still authenticating; nothing will notice when it lapses.                | Medium | S        |
| 10  | E7+E8+deps | Bump vitest + pool-workers (two majors, 8 of 10 advisories, runtime currency); stub the AI binding in tests. | Medium | M        |
| 11  | D5         | `MAX_PAGES` silently truncates above 1,000 commits/repo; never present a floor as a total.                   | Medium | S        |
| 12  | D2         | Test the `scheduled` handler.                                                                                | Medium | S        |
| 13  | T4         | Hero claims agents review PRs; nothing measures it. Drop or prove.                                           | Medium | S/M      |
| 14  | E6         | Dependabot config.                                                                                           | Medium | S        |
| 15  | D1         | Remove dead `/api/presets`.                                                                                  | Low    | S        |
| 16  | T7         | README/spec drift (cap default, test count, repo count, `og.ts`).                                            | Low    | S        |
| 17  | T8         | Misnamed www test and stale catch-all comment.                                                               | Low    | S        |
| 18  | T6+T9      | Two copy nits ("every figure links…", "counted as human").                                                   | Low    | S        |
| 19  | B5         | Timeout on share-card render.                                                                                | Low    | S        |
| 20  | E9         | Root `CLAUDE.md` with the rules that matter.                                                                 | Low    | S        |
| 21  | zod, TS    | Remaining majors, one PR each, last.                                                                         | Low    | M        |
| 22  | D3         | Front-end smoke test, once CI exists.                                                                        | Low    | M        |
| —   | E2         | Branch protection on `main`.                                                                                 | High   | OPERATOR |
| —   | B4         | Record domain renewal; auto-renew.                                                                           | High   | OPERATOR |
| —   | T2 (alt)   | Remove the second collaborator's write access on bv-mcp.                                                     | High   | OPERATOR |
| —   | B1 (alt)   | Calendar the token rotation for 2027-08.                                                                     | Medium | OPERATOR |

## 4. Top five, as ready-to-run prompts

Each is self-contained, bounded to one PR, and assumes the rules in the audit prompt: never deploy, never touch KV or secrets, tests stay hermetic, no new runtime dependency, every commit carries a `Co-Authored-By: Claude …` trailer because this repo is in its own receipts. Do not merge; the operator does.

### Prompt 1 — Method page: make Scope true and keep it true (T1, T2, T5)

```
In the macrohard.nz repo (Cloudflare Worker; static site in public/; hermetic vitest in test/), fix three sentences on public/method.html so they match the code and config, and add a test so the first cannot drift again.

1. Line 40 says "Three public repositories, last 90 days, refreshed every 30 minutes:" and then lists four. wrangler.jsonc GITHUB_REPOS has four. Change "Three" to "Four" and change "refreshed every 30 minutes" to "refreshed every 30 minutes by a scheduled job, and on the first visit after 45" (src/index.ts RECEIPTS_MAX_AGE_MS is 45 minutes; that is the guarantee the code makes with or without GITHUB_TOKEN).
2. Line 76 says "The repositories have one human maintainer with commit access." That is not provable from public data and is false for one repo (a second collaborator has write access, with no commits in the window). Reword to what the figures support: "One human has committed to these repositories in the window. That person holds the deploy keys and runs production deploys by hand — the agents open the release; they do not press the button." Keep the rest of the paragraph.
3. Add a test in test/assets.spec.ts: fetch /method through SELF, count the <a href="https://github.com/MadaBurns/…"> links inside the #scope list, and assert the count equals env.GITHUB_REPOS.split(',').length, and that the Scope paragraph's number word matches that count (a small map of 1..9 to words is fine). Read the existing tests in that file for the fetch pattern.

Do not touch src/, wrangler.jsonc, or any other page. Run: npm ci && npm run types && npm run typecheck && npm test && npm run format:check. Paste the tail of each in the PR body. Branch improve/method-scope-truth; one PR to main titled "Method page: four repositories, the refresh guarantee the code makes, and a provable headcount sentence". Append one dated line to docs/superpowers/2026-09-12-improvement-backlog.md under a new "## 5. Done" heading: item numbers, PR link, verification. Do not merge.
```

### Prompt 2 — CI and a green baseline on a clean clone (E1, E3, E4, E5)

```
In the macrohard.nz repo, make the baseline command chain green on a fresh clone and enforce it in CI. Today it fails twice: `npm run typecheck` fails with TS2688 until `wrangler types` has run (worker-configuration.d.ts is generated and gitignored), and `npm run format:check` fails on docs/superpowers/2026-09-12-e2e-results.md.

1. package.json: add "pretypecheck": "wrangler types" and "predeploy": "npm run typecheck && npm test". Deploy remains operator-run; the hook only refuses when red. Add one sentence to README "Running it" saying typecheck generates the types first.
2. Formatting: run prettier --write on docs/superpowers/2026-09-12-e2e-results.md. Diff the result; if any table or code block is damaged, revert that file and instead add `docs/superpowers/*.md` to .prettierignore with a one-line comment saying why. Either way `npm run format:check` must exit 0.
3. Add .github/workflows/ci.yml: on pull_request and on push to main; ubuntu-latest; actions/checkout and actions/setup-node with node-version 22 and npm cache; then exactly `npm ci`, `npm run typecheck`, `npm test`, `npm run format:check` as separate steps. No secrets, no deploy, no wrangler login. Pin action versions to a major tag.
4. Add .github/dependabot.yml: package-ecosystem npm, weekly, group minor and patch updates; majors stay separate.

Do not change src/, test/, wrangler.jsonc, or vitest.config.ts. Verify locally: rm -f worker-configuration.d.ts && npm ci && npm run typecheck && npm test && npm run format:check — all four must pass from that cold start. Paste the tails in the PR body. Branch improve/ci-baseline; one PR to main titled "CI: run the baseline chain, and make it pass on a clean clone". Append a dated Done line to docs/superpowers/2026-09-12-improvement-backlog.md (create "## 5. Done" if absent). Do not merge. Note in the PR body that branch protection requiring this check is an operator action.
```

### Prompt 3 — A guard outage must not mint a permalink (T3, L4)

```
In the macrohard.nz repo, close the gap between README's sentence "Nothing reaches a public permalink unmoderated" and the code. Today src/staff.ts moderateQuery and moderateOrg return 'unknown' when Llama Guard throws, and src/index.ts POST /api/staff treats anything but 'unsafe' as pass, then stores the org at s:<id>, caches it, and returns an id. An outage should not take the toy down, but it must not persist.

Change POST /api/staff in src/index.ts:
- Capture both verdicts. If either is 'unknown', still return the generated org (mode 'ai') but: do not write s:<id>, do not write the query cache, do not bump count:total, do not prerender the share image, and return id: null and a new field moderated: false. Only 'safe' on both guards persists.
- The fallback path (cap spent or model failure) is unchanged.
- public/app.js already removes Post and Copy when id is absent; confirm and do not change it beyond, at most, a one-line note text when moderated === false.

Tests (hermetic; inject a fake AI whose run() throws for the guard model and returns a valid org for the generator — see test/staff.spec.ts and test/moderation.spec.ts for the fake pattern):
- unknown query verdict → 200, org present, id null, nothing under s: or org: in KV, count:total unchanged.
- unknown org verdict → same.
- safe/safe → persists exactly as today (existing tests must still pass unchanged).

Update README "Spend controls" item 5 to say a guard outage serves a result without a permalink, and update the spec's Generator section to match. No new dependency, no wrangler.jsonc change. Run the baseline chain and paste the tails. Branch improve/guard-unknown-no-permalink; one PR to main titled "A guard outage serves a result but never mints a permalink". Append a dated Done line to docs/superpowers/2026-09-12-improvement-backlog.md. Do not merge.
```

### Prompt 4 — Permalinks must outlive the launch (B2)

```
In the macrohard.nz repo, src/index.ts stores every permalink (s:<id>) and its share image (og:<id>) with expirationTtl PERMALINK_TTL = 180 days. Every link shared at launch, including the three Day-0 permalinks in docs/superpowers/2026-09-12-e2e-results.md, will 404 from about 2027-03-11. A stored org is about 1 KB; KV storage is not the constraint.

Change: remove the expirationTtl from the s:<id> put (permanent). Keep a TTL on og:<id> but raise it to 365 days; the image re-renders from the stored org on a miss, bounded by the existing oglock: and ogcap: controls. Leave the 30-day query cache alone. Rename PERMALINK_TTL to OG_TTL so the name says what it now governs.

Tests: in test/routes.spec.ts or test/share.spec.ts, after a stored generation, assert via env.KV.getWithMetadata (or the KV list API's expiration field) that s:<id> has no expiration and og:<id> does. If the test pool's KV does not expose expiration, assert on the put call by spying on env.KV.put instead; say which you did in the PR body.

Update the design spec's Sharing section ("cached in KV for the permalink's lifetime") to match. No other files. Run the baseline chain, paste the tails. Branch improve/permalinks-permanent; one PR to main titled "Permalinks no longer expire; share images re-render on a miss". Append a dated Done line to docs/superpowers/2026-09-12-improvement-backlog.md. Do not merge.
```

### Prompt 5 — Test runtime currency: vitest 5 + pool-workers 0.22, AI binding stubbed (E7, E8, deps)

```
In the macrohard.nz repo, the hermetic test suite runs on a Workers runtime whose newest supported compatibility date is 2025-09-06 while wrangler.jsonc pins production to 2026-09-01, and npm audit reports 10 advisories, 8 of them via the nested miniflare/wrangler under @cloudflare/vitest-pool-workers 0.8.71. Also, the real AI binding is live inside the test pool (the pool warns it will bill); no current test reaches it, but nothing enforces that.

Do, in this order, one PR:
1. Read the vitest 4 and 5 migration notes and the @cloudflare/vitest-pool-workers changelog from 0.8 to 0.22 before editing anything. Note any change to defineWorkersConfig, fetchMock, cloudflare:test exports, or the wrangler peer range.
2. Bump vitest to ^5.0.0 and @cloudflare/vitest-pool-workers to ^0.22.0 together. Do not bump zod, typescript, wrangler, hono, or @cloudflare/puppeteer in this PR.
3. In vitest.config.ts, override the AI binding so any call bills nothing and fails loudly: use the pool's miniflare options to replace AI with a stub whose run() throws "AI binding is stubbed in tests; inject a fake". All 84 existing tests must still pass without modification other than what the migration itself requires; the /api/staff route tests only hit the rejected and cached paths, so they should be unaffected.
4. Confirm the [mf:warn] about the compatibility date is gone and the [vpw:warn] about Workers AI billing is gone from `npm test` output. If the newest runtime still trails 2026-09-01, record the exact supported date in the PR body rather than lowering wrangler.jsonc.
5. Run npm audit and paste the before/after counts. The extract-zip advisories under @cloudflare/puppeteer will remain: say so and why (local browser download path, not Worker code; the offered fix is a downgrade).

Run the baseline chain from a cold start (rm -f worker-configuration.d.ts first), paste the tails. Branch improve/test-runtime-currency; one PR to main titled "Tests: vitest 5, pool-workers 0.22, current runtime, AI binding stubbed". Append a dated Done line to docs/superpowers/2026-09-12-improvement-backlog.md. Do not merge.
```
